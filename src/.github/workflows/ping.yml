name: Keep Render Alive

on:
  schedule:
    - cron: '0 */2 * * *'  # 每 2 小时 ping 一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render project
        run: curl -s https://jobresearch-backend.onrender.com || true
